# Force Qt apps to use Wayland platform plugin.
# Prevents "no Qt platform plugin could be initialized" crashes in
# dbus-activated apps (e.g. plasma-discover opened via URL handler).
QT_QPA_PLATFORM=wayland
